section.main-page__last.last
  h2.last__name
    | Последние комментарии
    |
    span.last__icon.last__icon--cloud
  if comments && comments.length
    ul.last__list
      each comment in comments
        li.last__list-item
          img.last__list-image(src=`img/${comment.account.avatar}` width='20' height='20' alt='Аватар пользователя')
          b.last__list-name= `${comment.account.firstname} ${comment.account.lastname}`
          a.last__list-link(href=`/articles/${comment.articleId}`)= comment.text
  else
    p.hot__empty Здесь пока ничего нет...
